const WorkbenchUtils=(()=>{let toastContainer;return{init(){if(!document.getElementById('toast-container')){toastContainer=document.createElement('div');toastContainer.id='toast-container';toastContainer.className='fixed bottom-4 right-4 z-[9999] space-y-2';document.body.appendChild(toastContainer)}console.log('[Utils] ✅ Initialized');return true},toast(msg,type='info',dur=3000){if(!toastContainer)this.init();const colors={success:'bg-green-600',error:'bg-red-600',warning:'bg-yellow-600',info:'bg-blue-600'};const toast=document.createElement('div');toast.className=`${colors[type]||colors.info} text-white px-6 py-3 rounded-lg shadow-xl`;toast.textContent=msg;toastContainer.appendChild(toast);setTimeout(()=>toast.remove(),dur)},escapeHtml(t){const d=document.createElement('div');d.textContent=t;return d.innerHTML},generateId(p='id'){return `${p}_${Date.now()}_${Math.random().toString(36).substr(2,9)}`},formatNumber(n,d=0){const num=typeof n==='number'?n:parseFloat(n)||0;return num.toLocaleString('zh-CN',{minimumFractionDigits:d,maximumFractionDigits:d})}}})();window.WorkbenchUtils=WorkbenchUtils;
