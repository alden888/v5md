const WorkbenchDashboard=(()=>{let clockTimer;return{init(){console.log('[Dashboard] ✅ Initialized');return true},startClock(){clockTimer=setInterval(()=>{const el=document.getElementById('current-time');if(el){const now=new Date();el.textContent=now.toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false})}},1000)},renderDashboard(){try{const orders=WorkbenchStorage.load('orders')||[];const incomes=WorkbenchStorage.load('incomes')||[];const suppliers=WorkbenchStorage.load('suppliers')||[];const expenses=WorkbenchStorage.load('expenses')||[];const totalIncome=incomes.reduce((s,i)=>s+(Number(i.amount)||0),0);const now=new Date();const monthIncome=incomes.filter(i=>{const d=new Date(i.date||i.createTime);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear()}).reduce((s,i)=>s+(Number(i.amount)||0),0);const el=(id,txt)=>{const e=document.getElementById(id);if(e)e.textContent=txt};el('dashboard-total-orders',orders.length);el('dashboard-total-income',`¥${totalIncome.toFixed(2)}`);el('dashboard-month-income',`¥${monthIncome.toFixed(2)}`);el('dashboard-total-suppliers',suppliers.length);console.log('[Dashboard] ✅ Rendered')}catch(e){console.error('[Dashboard] Render failed:',e)}}}})();window.WorkbenchDashboard=WorkbenchDashboard;
